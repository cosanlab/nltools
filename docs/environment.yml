channels:
  - conda-forge

name:
  - nltools >= 0.4.2

dependencies:
  - python >= 3.7
  - pip:
    - sphinx >= 1.5
    - sphinx-gallery
    - sphinxcontrib-napoleon
    - sphinx_bootstrap_theme
    - networkx
